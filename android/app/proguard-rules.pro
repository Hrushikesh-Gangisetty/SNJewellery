# R8 rules for the release build.
#
# Empty on purpose. AGP's default rules plus the consumer rules shipped
# by AndroidX and Compose cover everything this module currently uses.
#
# Add a rule here only with a comment naming the library that needs it
# and the symptom without it — an unexplained -keep is impossible to
# retire later, and they accumulate.
